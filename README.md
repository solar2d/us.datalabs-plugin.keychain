Sources for the plugin `plugin.keychain`.

Add following to your `build.settings` to use:
```lua
{
    plugins = {
        "plugin.keychain" = {
            publisherId = "us.datalabs",
        },
    },
}
```
